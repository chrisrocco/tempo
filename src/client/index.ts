/**
 * @fileoverview
 * CLIENT — handles over one execution, plus the server-wide reads, over any
 * `WorkflowService`. `createRemoteClient` adds the one call that only makes sense
 * against a server in another process.
 */

export * from './client';
